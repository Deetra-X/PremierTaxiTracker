PORT=3000
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require&channel_binding=require
JWT_SECRET=change_me_later
NODE_ENV=development

# JWT hardening
JWT_ISSUER=tuk-tuk-api
JWT_AUDIENCE=tuk-tuk-web
JWT_EXPIRES_IN=1h
JWT_ALG=HS256

# CORS (comma-separated). Leave empty for default-deny.
# Example: CORS_ORIGIN=https://your-frontend.com,https://admin.your-frontend.com
CORS_ORIGIN=

# Docs (Swagger/OpenAPI)
# In production, keep this false by default.
# If true, docs are enabled. In production they are HQ_ADMIN-only unless PUBLIC_API_DOCS=true.
ENABLE_API_DOCS=true
PUBLIC_API_DOCS=false

# Testing only (do not set in production unless intentional): lowers global HTTP rate limit
# TEST_GLOBAL_RATE_LIMIT=5

# Tracking privacy controls
LOCATION_PRECISION_DECIMALS=5
ENABLE_NIC_SEARCH=false
INCLUDE_DRIVER_NIC_IN_RESPONSE=false

